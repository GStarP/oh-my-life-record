# 票据分流标签

技能中的五种分流角色与本仓库使用的标签相同：

| 标签 | 含义 |
| --- | --- |
| `needs-triage` | 等待维护者评估 |
| `needs-info` | 等待提出者补充信息 |
| `ready-for-agent` | 规格完整，可以交给 agent 执行 |
| `ready-for-human` | 需要人工实现 |
| `wontfix` | 不再处理 |

如果技能要求使用某个角色标签，直接使用上表中对应的字符串。
