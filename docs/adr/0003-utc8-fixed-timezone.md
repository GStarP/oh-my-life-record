# 固定 UTC+8 作为时间归属与展示基准

记录时间的绝对瞬间使用 `Date` 表示（见 ADR-0006）；月份归属、日分组、输入和展示一律按固定 UTC+8 计算，不随设备时区变化。

原因：分片按「记录所属月份」划分，若时区随设备漂移，同一时刻的记录在 A 设备归 8 月、在 B 设备归 7 月，会导致分片归属和同步结果不一致。固定 UTC+8 同时保证跨设备和旅行场景下的分片、日分组与显示稳定。

代价：用户在非 UTC+8 时区旅行时，时间显示与实际当地时间可能不同；这是刻意接受的产品取舍。
