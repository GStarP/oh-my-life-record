# 冲突只允许云端覆盖（无合并、无本地回推）

同步按钮是唯一写入入口：云端版本更高则下载覆盖本地；版本一致且有本地修改则上传。冲突（云端更高且本地有未上传修改）时，任一冲突全局封锁本次同步（一票否决），须用户确认以云端覆盖冲突月后整个同步才继续；取消则整体中止。启动/回前台仅只读检查，不拉取数据。

放弃了记录级合并与「本地优先上传」的可能性，换取同步逻辑的完全确定性——系统永远只有两种数据流向：版本一致时上传、版本不一致时下载覆盖。任何并发编辑的本地改动都注定被云端覆盖，这是刻意接受的代价。
