# 个人生活记录（Oh My Life）

单用户个人生活记录应用。IndexedDB 是业务数据的唯一事实来源（数据库）；S3 兼容对象存储（Cloudflare R2）保存云端快照与图片。无用户体系，默认不同设备不同时编辑。

本文只维护领域词汇和跨模块不变的规则。当前产品范围与运行架构见 `docs/设计文档.md`，不可逆决策见 `docs/adr/`，桶内对象格式见 `docs/cloud-storage-format.md`。

## 语言

### 同步与云端

**分片（Partition）**:
按月（UTC+8 自然月）划分的数据单元，对应云端 `records/YYYY-MM.json`。是上传、下载、冲突判定、版本跟踪的最小单位。
_Avoid_: 月份文件、月度文件（实现细节）

**云端快照（Cloud Snapshot）**:
S3 上的某分片 JSON。不是数据库，是本地数据的可导出视图；恢复时以整分片覆盖本地。

S3/R2 对象的实际键名、JSON 字段和图片引用关系统一见项目文档 `docs/cloud-storage-format.md`；该文档属于项目维护资料，不上传到桶内。

**分片替换（Partition Replace）**:
将云端分片整体替换本地对应分片：同一事务内删除本地该月全部记录、写入云端该月全部记录、更新该月同步状态。原子生效或整体回滚。与上传方向对称，不增量、不合并。

**版本号（Revision）**:
某个同步单元的单调递增整数：记录分片按月份独立计数，类型模板集合使用一个全局计数。每次成功上传对应单元后云端值 +1；本地记录最近一次确认的云端值。
_Avoid_: 时间戳、哈希（不使用）

**冲突（Conflict）**:
某分片同时满足「本地版本 ≠ 云端版本」且「本地存在未上传修改」。冲突只能经用户确认后以云端覆盖本地解决，不存在其他出路。
类型模板集合也维护一个全局版本；模板冲突与月份冲突一起触发全局确认。

**云端优先（Cloud-First）**:
上传规则：仅当分片「本地版本 == 云端版本」且为未上传修改状态时允许上传；其余任何情况（含冲突）必须先以云端覆盖本地。本地修改永远不会覆盖云端已存在的版本。

**同步按钮（Sync Button）**:
上传与下载的统一入口，用户主动触发；启动/回前台只读检查不拉取数据，下载仅发生于此按钮。已完成云端配置且云端可达时用图标 + 文案显示「已同步 / 待上传 / 有更新 / 需同步」；显示「已同步」时点击只重新检查，不在同一次点击中继续同步。同步指示器是应用级状态，由根布局启动的后台只读监视器维护，路由切换不会重置；监视器在应用启动、回到前台、恢复联网和定时间隔检查 manifest，但不会自动上传或下载。未完成配置、云端不可达、离线或同步进行中时禁用或由全局云端配置弹窗阻断，重新验证成功后才恢复使用。
_Avoid_: 上传按钮（下载也走这里）、同步（泛指检查或拉取）

**未上传修改（Unuploaded Change）**:
本地已存在但尚未上传到云端的分片或类型模板集合修改。记录仍只维护分片级布尔标记，模板集合维护一个全局布尔标记，不区分记录级明细。


**记录操作（Record Operation）**
`record-workflow` 提供记录保存和删除入口，同时维护对应 UTC+8 月份的未上传修改状态（跨月改时间标两个月），并在写操作成功后清理本地孤儿图片。`cloud/sync/engine` 只负责全局云端同步；记录页在成功下载后负责刷新页面数据和清理本地孤儿图片。
_Avoid_: 让 UI 直接写 partitionState

**同步否决（Sync Veto）**:
任一冲突存在时，同步按钮整体不可执行，须经用户确认以云端覆盖冲突月份或类型模板后整个同步才继续；取消则整体中止。一票否决，不分月。

**云端凭证（Cloud Credential）**:
用户提供的 R2 端点、桶名与访问密钥，用于前端本地生成签名请求（SigV4）。仅存于用户浏览器，不写入代码或仓库。首次启动无凭证 → 全屏**云端凭证配置弹窗**（凭证无效或网络不可达时均禁止绕过）；之后由设置页入口打开。应用不提供离线工作流，云端异常时保持配置弹窗阻断使用。
_Avoid_: 密钥、key（与记录属性含义冲突）

**本地签名 URL（Locally Signed URL）**:
前端用云端凭证临时生成的私有桶 GET URL，用于展示图片，生成后短期有效。
_Avoid_: 公开 URL、预签名（未使用服务端签发）

### 数据模型

**记录（Record）**:
一条生活记录：不可变 ID、时间（Date 对象存储，绝对瞬间；月份归属与展示按 UTC+8，见 ADR-0003/0006）、类型文本（也可以为空）、可选描述、图片 ID 列表、自由属性（string/number/boolean）。记录表单可以处于模板表单状态或自由表单状态；记录本身不保存类型模板副本。
_Avoid_: 条目、event

**类型（Type）**:
记录上的自由文本分类，也可以为空。类型本身不携带属性定义；只有命中类型模板的模板表单状态才会锁定类型，自由表单仍可按原有方式修改类型。

**类型模板（Record Type Template）**:
可复用的记录表单预设，以类型文本为唯一名称，包含一个图标字符串标识和一组属性定义。一个类型最多一个模板；模板类型不可编辑，要改类型只能删除旧模板后重新创建；不提供独立模板名称，也不支持调整属性顺序。图标只能从应用提供的固定图标集合中选择，模板和记录不保存图标组件本身。表单在渲染前收到类型文本且查到模板时进入模板表单状态：显示模板属性（即使当前没有存储值），模板预置属性只允许修改值，用户额外添加的属性可以删除。模板更新不改变已保存的属性值；已存在但不再被模板定义的属性保留原位置并退回自由属性输入。类型模板属于用户数据，需要在设备间同步。
_Avoid_: 记录模板（容易误解为记录仍与模板保持关联）

**模板表单状态（Template Form State）**:
表单渲染前收到类型文本且匹配到类型模板时的表单状态。类型和模板预置属性不可修改其结构，只能修改属性值；额外属性仍可按自由属性规则编辑，且只能使用文本、数值、布尔三种基础属性类型。

**自由表单状态（Free Form State）**:
没有预传类型，或预传类型没有匹配到类型模板时的表单状态。沿用原有自由记录表单，类型和自定义属性均可编辑。

**属性定义（Attribute Definition）**:
类型模板中对一个自定义属性的预设描述，包括属性名和一种用户可理解的属性类型：文本、数值、布尔或选项。用户不需要分别配置存储类型和录入方式；选项保存字符串。

**属性类型变更（Attribute Kind Change）**:
修改模板中的属性类型不会批量改写历史记录，也不建立额外的领域级转换规则；当前版本先由表单输入组件处理已有值，遇到实际边界后再补充规则。

**模板属性类型（Template Attribute Kind）**:
类型模板中自定义属性的四种互斥类型：文本、数值、布尔、选项。文本与选项都保存字符串；布尔沿用自由表单的基础输入；选项使用可输入字符串的 Combobox。

**选项属性（Option Attribute）**:
基础值类型为 string、录入时使用 Combobox 的属性。预设选项列表可以为空；预设选项既可供选择也可供过滤，用户也可以输入列表外的字符串；输入值就是实际保存的字符串，不另外维护隐藏值，也不因输入新值自动修改模板。同一列表中不应出现重复的非空选项字符串，模板保存时遇到重复选项应阻止保存。

**空属性值（Empty Attribute Value）**:
文本和数值属性为空时，保存记录时不写入该属性；这与记录没有该属性视为同一种状态。布尔属性始终有值；数值的 0 是明确保存的数值，不视为空。

**图片（Image）**:
独立于记录存储于 S3 的对象，记录中仅引用其 ID。图片是不可变对象，ID 全局唯一，S3 只存一份。图片仅来自相册选择，无拍摄概念；通常上传压缩后的 WebP，极少数浏览器无法处理原始格式时保留原始二进制。

**图片引用（Image Reference）**:
记录通过不可变图片 ID 引用图片；同一图片可被多条记录共享（去重），不复制对象。

**待上传图片（Pending Image）**:
已被本地记录引用、但尚未上传到云端的图片。仅在手动上传动作中随记录一起上传，绝不自动上传。

**图片暂存区（Image Staging）**:
本地仅存放「尚未成功上传」的图片，存于 IndexedDB 的图片对象存储（id → Blob + createdAt 元数据）。对外仍以 id → Blob 读取；createdAt 只用于启动兜底清理。上传成功后即从暂存区删除；存在即待上传。
_Avoid_: 本地图片仓库、缓存（暂存区只放待上传，不积累已上传图片）

**本地孤儿图片（Local Orphan）**:
无任何本地记录引用的暂存图片（选图后关闭未提交的新建内容、或记录在上传前被删除所致）。关闭未提交内容/删除记录时立即清理；启动时兜底清理「未引用且超过 7 天」者。

**云端孤儿图片（Cloud Orphan）**:
未被任何本地记录引用的云端图片。来源：记录删除、从记录移除图片、或各类异常。不随删除操作隐式处理，仅由用户手动触发的「图片清理」功能扫描并删除；设置页入口文案为「清理未引用图片」；未触发则一直留存于云端。

**图片上传状态（Image Upload Status）**:
由「是否存在本地暂存区」推断：存在即待上传，上传成功后删除本体即脱离待上传集合。仅用于计算上传集合（被引用且未上传），不用于显示路由。

**图片清理（Image Cleanup）**:
用户手动触发的功能（设置页入口文案为「清理未引用图片」）：扫描本地全部记录与云端全部图片，删除未被任何记录引用的云端图片（确认后执行）。不自动触发，不处理本地暂存区。

**图片显示与图片管理（Image Display / Image Manager）**:
图片展示流程、Cache Storage、Object URL、应用级唯一 `ImageManager` 及页面状态边界属于运行架构，统一见 `docs/设计文档.md` 第 6 节和第 9 节；不在此重复实现细节。

**应用级状态（Application State）**:
跨页面或后台逻辑需要共享的数据使用应用级状态；表单草稿、筛选输入和弹窗开关保留在页面或组件内。完整的状态归属和 keep-alive 规则见 `docs/设计文档.md` 第 9 节。
